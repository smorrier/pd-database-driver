{
  "name": "pg-database-driver",
  "version": "1.3.3",
  "description": "Simple CRUD query builders for postgres",
  "main": "src/index.js",
  "scripts": {
    "build": "rm -r dist && tsc -p . && cp package.json dist && cp README.md dist",
    "deploy": "yarn version && npm run build && cd dist && npm publish"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.5.12",
    "@types/pg": "^8.6.1",
    "@types/uuid": "^8.3.1",
    "@typescript-eslint/eslint-plugin": "^4.27.0",
    "@typescript-eslint/parser": "^4.29.0",
    "eslint": "^7.32.0",
    "pg": "^8.7.1",
    "typescript": "^4.3.5"
  }
}
